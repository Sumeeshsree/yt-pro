{
    "buildCommand": "prisma generate && next build",
    "crons": [
        {
            "path": "/api/cron/update-analytics",
            "schedule": "0 */6 * * *"
        }
    ],
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "/api/:path*"
        }
    ]
}